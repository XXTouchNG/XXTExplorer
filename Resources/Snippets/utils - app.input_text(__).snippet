local group, name

group = '辅助'
name = 'app.input_text("")'

return {
	name = string.format('%s - %s', group, name),
	arguments = {
	},
	generator = function()
		return string.format('app.input_text("@@")')
	end,
}