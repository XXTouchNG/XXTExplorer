local group, name

group = 'App'
name = 'app.run(bid)'

return {
	name = string.format('%s - %s', group, name),
	arguments = {
		{type = '@app@'},
	},
	generator = function(bid)
		return string.format('app.run(%q)', bid)
	end,
}