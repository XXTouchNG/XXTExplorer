local group, name

group = '辅助'
name = 'sys.toast("")'

return {
	name = string.format('%s - %s', group, name),
	arguments = {
	},
	generator = function()
		return string.format('sys.toast("@@")')
	end,
}